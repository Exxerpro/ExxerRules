# Epic: EXXER101 - UseXUnitV3 Analyzer False-Positive Mitigation

**Analyzer ID**: `EXXER101`  
**Source**: `src/code/IndFusion.Analyzer/Testing/UseXUnitV3Analyzer.cs`  
**Prepared by**: Codex agent (2025-10-07)

## Definition of Ready

- [ ] Sufficient context about the implementation has been collected.
- [ ] The document has been updated with a detailed plan.
- [ ] All dependencies and potential blockers have been identified.
- [ ] The team has reviewed and agreed upon the plan.

## Definition of Done

- [ ] All stories are complete and meet their acceptance criteria.
- [ ] All new regression tests are added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build warnings treated as errors, and 0 failing tests on all the test suite.
- [ ] Documentation updated (this spec + release notes).
- [ ] The project builds successfully without any new warnings or errors.

---

## Stories

### 1.1. Story: Skip Auto-Generated Microsoft Testing Platform Files

**As a** developer  
**I want** the analyzer to ignore auto-generated files from the Microsoft Testing Platform  
**So that** I don't get false positives from generated code.

#### 1.1.1. Acceptance Criteria

**Given** a file is marked with `<auto-generated>` or is located in an `obj` or `bin` directory  
**When** the analyzer runs  
**Then** no diagnostic should be reported for that file.

#### 1.1.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.2. Story: Allow Microsoft Testing Platform Builder Hooks

**As a** developer  
**I want** the analyzer to allow references to `Microsoft.Testing.Platform` builder hooks  
**So that** I can use the modern testing platform with xUnit v3 without warnings.

#### 1.2.1. Acceptance Criteria

**Given** a file uses attributes or namespaces from `Microsoft.Testing.Platform`  
**When** the analyzer runs  
**Then** no diagnostic should be reported.

#### 1.2.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.3. Story: Ignore Compatibility Fixtures for Migration Proofs

**As a** developer  
**I want** to be able to ignore specific projects from this analyzer's checks  
**So that** I can have compatibility fixtures that reference MSTest for migration testing purposes.

#### 1.3.1. Acceptance Criteria

**Given** a project is listed in the `dotnet_diagnostic.EXXER101.ignore_projects` configuration  
**When** the analyzer runs on that project  
**Then** no diagnostic should be reported.

#### 1.3.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.4. Story: Permit Local Compatibility Assemblies

**As a** developer  
**I want** the analyzer to permit local compatibility shims for test frameworks  
**So that** I can create bridging layers for migrating tests.

#### 1.4.1. Acceptance Criteria

**Given** an attribute that looks like an MSTest or NUnit attribute is defined within the current solution  
**When** the analyzer runs  
**Then** no diagnostic should be reported for its usage.

#### 1.4.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.5. Story: Allow `Microsoft.Testing.Platform` Namespaces

**As a** developer  
**I want** the analyzer to allow `using` directives for `Microsoft.Testing.Platform` namespaces  
**So that** auto-generated entry points for xUnit v3 are not flagged.

#### 1.5.1. Acceptance Criteria

**Given** a file contains a `using Microsoft.Testing.Platform.*` directive  
**When** the analyzer runs  
**Then** no diagnostic should be reported.

#### 1.5.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.6. Story: Distinguish Between Code and Strings/Comments

**As a** developer  
**I want** the analyzer to ignore MSTest/NUnit syntax inside strings and comments  
**So that** I can have documentation or samples without getting false positives.

#### 1.6.1. Acceptance Criteria

**Given** a file contains MSTest or NUnit attributes or keywords within string literals or comments  
**When** the analyzer runs  
**Then** no diagnostic should be reported.

#### 1.6.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.7. Story: Provide Configuration to Allow Specific Legacy Assemblies

**As a** developer  
**I want** to be able to suppress the analyzer for specific legacy assemblies  
**So that** I can maintain legacy test projects without being forced to migrate them immediately.

#### 1.7.1. Acceptance Criteria

**Given** an assembly is marked with a `[SuppressTestFrameworkConversion("...")]` attribute  
**When** the analyzer runs on that assembly  
**Then** no diagnostic should be reported.

#### 1.7.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.8. Story: Ignore Analyzer Test Assets

**As a** developer of the analyzer  
**I want** the analyzer to ignore its own test assets  
**So that** the analyzer doesn't flag the test cases used to validate it.

#### 1.8.1. Acceptance Criteria

**Given** a file is part of the analyzer's own test project (e.g., `IndFusion.Analyzer.Tests`)  
**When** the analyzer runs  
**Then** no diagnostic should be reported.

#### 1.8.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.9. Story: Avoid Double-Reporting on Transitive `using` Statements

**As a** developer  
**I want** the analyzer to report a `global using` statement only once  
**So that** I don't get duplicate warnings in every file.

#### 1.9.1. Acceptance Criteria

**Given** a project uses a `global using` for an MSTest or NUnit namespace  
**When** the analyzer runs  
**Then** the diagnostic should be reported only once for the `global using` statement, not in every file that consumes it.

#### 1.9.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).

---

### 1.10. Story: Provide Severity Downgrade for Transitional Projects

**As a** developer  
**I want** to be able to downgrade the severity of the analyzer's diagnostics  
**So that** I can see them as suggestions rather than blocking warnings during a migration period.

#### 1.10.1. Acceptance Criteria

**Given** the `.editorconfig` file sets the severity for `EXXER101` to `suggestion` or `silent`  
**When** the analyzer runs  
**Then** the reported diagnostics should have the configured severity.

#### 1.10.2. Acceptance Checklist

- [ ] Analyzer heuristics enhanced for all scenarios.
- [ ] All new regression tests added and passing.
- [ ] Build/test pipelines succeed (`dotnet build`, `dotnet test`). Zero build test with warning as error treated, 0 failing test on all the test suite.
- [ ] Documentation updated (this spec + release notes).